Exepected Results

Bad Messages
  When unexpected behavior happens within an application. Do not return 500 STATUS CODE or STACK TRACE in the server response. Instead, return a 400 Bad Message. Can either leave the message generic or custom.


Committing directly to the Main branch is bad practice (unless you're on the contributor list)
- red flag if there aren't any other branches under (code > main - dropdown)
- Should have a pull request at minimum if they do directly commit to main

Supply Chain attacks - Application Security Program needs to develop a process that will prevent developers from downloading whatever 3rd party libraries without a process/scanning

Need to keep a list/inventory of technologies within a tech stack to keep an eye on.
